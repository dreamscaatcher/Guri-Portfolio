apiVersion: v1
kind: Service
metadata:
  name: guri-portfolio-service
spec:
  type: LoadBalancer
  selector:
    app: guri-portfolio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
